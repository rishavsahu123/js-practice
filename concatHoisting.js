var temp = "rishav"
// concat return updated data, it doesn't append in existing.
temp.concat(' sahu')
console.log(temp)

arr = [1, 2, 3, 4]
arr = arr.concat([3, 4, 4, 4])
console.log(arr)
var arr; //hoisting
